# https://pub.dev/packages/pedantic_mono
include: package:pedantic_mono/analysis_options.yaml

linter:
  rules:
    # CLI でログ出力するので、print を許可する。
    # logger や debugPrint をうまく使えなかったため、print を使う。
    avoid_print: false